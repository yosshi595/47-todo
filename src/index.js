import React from 'react'
import ReactDOM from 'react-dom'

import App from './App'

ReactDOM.render(<App />, document.getElementById('root'))
// React は SPA(シングルページアプリケーション)
// 一つのhtmlを読み込んで、コンテンツの入れ替えとかページ遷移とかを全てJSで処理していく